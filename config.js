const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "", //sizin şifreniz
        database: "" //oluşturduğunuz veritabanının adı
    }
}

module.exports = config;